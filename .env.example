# Example .env organized to match `secure.py` sections.
# All current values are preserved exactly as in the original file.
# Optional settings remain commented.

### Secrets (required)
# Django secret key (required)
DJANGO_SECRET_KEY=1@7&11tb*l1c84uco-9=%(u#mb)_dl6%%++rihgnl&r)wmldrc
# LTI / consumer
CONSUMER_KEY=academic_integrity_tool_v2
LTI_SECRET=secret

### Database (required)
DB_HOST=db
DB_NAME=academic_integrity_tool_v2
DB_USER=academic_integrity_tool_v2
DB_PASSWORD=academic_integrity_tool_v2
# Optional DB port (commented)
# DB_PORT=5432

### Caching / Redis
REDIS_HOST=redis
# Optional Redis port
# REDIS_PORT=6379
# Cache TTL (optional)
# DEFAULT_CACHE_TIMEOUT_SECS=300

### Logging
# Log level (optional)
# LOG_LEVEL=INFO

### Runtime
# Enable debug
ENABLE_DEBUG=True
# Which settings module to use (optional locally docker-compose passes in the local file)
# DJANGO_SETTINGS_MODULE=academic_integrity_tool_v2.settings.local

### Security
# Hosts allowed
# Ngrok used to expose local servers to the internet (https) and avoid CORS issues
# See README, https://ngrok.com/docs for more information
ALLOWED_HOSTS=127.0.0.1,localhost,.ngrok-free.app
# X-Frame-Options
X_FRAME_OPTIONS=ALLOW-FROM https://harvard.beta.instructure.com

### Session / cookie management
# Help keep session cookie settings
# DJANGO_SESSION_COOKIE_SECURE=True
# DJANGO_SESSION_COOKIE_SAMESITE=None

### Misc / notes
HELP_EMAIL_ADDRESS=atg@fas.harvard.edu

# Keep any additional optional overrides commented below
